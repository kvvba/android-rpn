<resources>

    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="MyButton">
        <item name="android:background">@null</item>
        <item name="android:textSize">@dimen/button_text_size</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">@dimen/button_layout_margin</item>
    </style>

    <!-- Container for a button with a secondary (hold) function: holds a MyTwoTierButtonPrimary
         plus a MyTwoTierButtonSecondary, so the primary label stays centered exactly like a
         plain MyButton instead of shifting down to make room for the secondary line. -->
    <style name="MyTwoTierButton">
        <item name="android:background">@null</item>
        <item name="android:padding">0dp</item>
        <item name="android:layout_margin">@dimen/button_layout_margin</item>
    </style>

    <style name="MyTwoTierButtonPrimary">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">22sp</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="MyTwoTierButtonSecondary">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">top|center_horizontal</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:alpha">0.7</item>
        <item name="android:fontFamily">monospace</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
